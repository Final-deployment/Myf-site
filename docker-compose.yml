services:
  app:
    build: .
    image: mastaba-v4-image
    container_name: mastaba-v4-container
    restart: always
    ports:
      - "3001:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    extra_hosts:
      - "smtp.gmail.com:142.251.127.109"
